import React from "react";

const appTable = ({ rowData, rowColums }) => {
  return (
    <table>
      {/* {
          for(let count = 0 ; count++; count<=rowData.length){


          }
      } */}
    </table>
  );
};
